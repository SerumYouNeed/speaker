# speaker
A simple client-server chat application written in C using TCP sockets. The server supports multiple clients concurrently (via multithreading or select), allowing real-time communication between connected users.
